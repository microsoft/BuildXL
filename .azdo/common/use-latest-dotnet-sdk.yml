parameters:
- name: sdkVersion
  type: string
  default: '9.x'

steps:
- task: UseDotNet@2
  displayName: Use .NET Core sdk ${{ parameters.sdkVersion }}
  inputs:
    version: ${{ parameters.sdkVersion }}

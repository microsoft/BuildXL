import {Transformer} from "Sdk.Transformers";

@@public
export const tool = toolTemplate.override<Transformer.ToolDefinition>({
    exe: f`../Daemon.Bin/SymbolDaemon.exe`, 
    runtimeDependencies: globR(d`../Daemon.Bin`, "*.*")
});
